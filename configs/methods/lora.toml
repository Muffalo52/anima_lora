# LoRA family — method selection via toggle blocks.
#
# Default (uncommented) = LoRA + OrthoLoRA + T-LoRA

# Routing config is on the three-axis surface
# Per-variant dim/alpha/lr/epochs are experiment drift, not calibrated
# defaults — tune freely.
# 90, 128, 152, 180, 214 each for 16, 32, 48, 64, 96
<<<<<<< HEAD

network_alpha = 128
network_dim = 32

learning_rate = 2e-5
max_train_epochs = 8
save_every_n_epochs = 8
checkpointing_epochs = 8
=======
network_alpha = 90
network_dim = 16

learning_rate = 5e-5
max_train_epochs = 4
save_every_n_epochs = 4
checkpointing_epochs = 4
>>>>>>> parent of f9273b69 (autoscale removal)

use_shuffled_caption_variants = true
# use_randomized_caption_variants = true
cache_llm_adapter_outputs = true
caption_dropout_rate = 0.1

<<<<<<< HEAD
output_name = "anima_lora"
=======
output_name = "anima_down_plain_tenth"
# sigmoid_bias = 0.5
# path_pattern = "sweetonedollar/*|ama_mitsuki/*|hews/*"
# artists_shard="3_16"
# vr_loss_weight = 1.0
# vr_fei_sigma_low_div = 4.0
# use_cmmd=true
# compile_inductor_mode = "max-autotune"

autoscale_mode = "curriculum"   # "none" | "curriculum" | "random"

# -------------
# Ortho-lora

>>>>>>> parent of f9273b69 (autoscale removal)
down_init = "weight_svd"
path_pattern = "*"

use_timestep_mask = true
min_rank = 1
alpha_rank_scale = 1.0


use_repa       = true
repa_mode      = "relational"
repa_weight    = 0.05          # settles at ~4% of total loss at plateau
repa_layer     = 8            # block to hook (of 28)
repa_encoder   = "pe_spatial"
<<<<<<< HEAD
=======
repa_lr_scale  = 1.0          # head LR multiplier (absolute mode only)
repa_spatial_norm = false
# repa_anneal_steps = 0.5

# Lever 1 — hard cutoff (HASTE): (0,1] = fraction of the run, >1 = absolute
# optimizer steps, 0 = off. Candidate: 0.25 — the quarter-run align_loss curve
# plateaus by ~18% of the run (anima_repa_normed_quarter, 2026-06-12).
# repa_anneal_steps = 0.25
>>>>>>> parent of f9273b69 (autoscale removal)

