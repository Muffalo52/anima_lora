# ResShift SR sidecar — isolated env deps (kept out of the main Anima lockfile so the
# SR closure doesn't pollute it; see sr/README.md). ResShift source is vendored under
# sr/resshift/ (basicsr-free), so basicsr is NOT a dependency anymore.
# NOTE: torch/torchvision installed separately from the cu132 index (Blackwell sm_120),
# matching the root .venv. xformers is intentionally OMITTED — the vendored VQGAN ships
# query-chunked SDPA attention; a Blackwell xformers build is a non-win.
scipy
numpy
opencv-python
matplotlib
timm
pandas
pillow
scikit-learn
scikit-image
lpips
loguru
omegaconf
six
tqdm
albumentations
einops
imageio
loralib
pyiqa
